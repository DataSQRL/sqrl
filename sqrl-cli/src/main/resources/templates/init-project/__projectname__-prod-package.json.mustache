{
  "version": "1",
  "enabled-engines": [{{{prod-engines}}}],
  "script": {
    "main": "{{project-name}}.sqrl",
    "config": {
      "source-type": "prod"
    }
  },
  "engines": {
    "flink": {
      "config": {
        "execution.runtime-mode": "{{exec-mode}}"
      }
    }
  }
}
