Orders.products := SELECT p.* FROM _ INNER JOIN _.entries e INNER JOIN Product AS p ON e.productid = p.productid;

LogicalProject(_uuid=[$11], _ingest_time=[$12], productid=[$13], name=[$14], description=[$15], category=[$16])
  LogicalJoin(condition=[=($7, $13)], joinType=[inner])
    LogicalJoin(condition=[=($0, $5)], joinType=[inner])
      LogicalTableScan(table=[[orders$12]])
      LogicalTableScan(table=[[entries$13]])
    LogicalTableScan(table=[[product$10]])


SELECT "product$10"."_uuid", "product$10"."_ingest_time", "product$10"."productid", "product$10"."name", "product$10"."description", "product$10"."category"
FROM "orders$12"
 INNER JOIN "entries$13" ON "orders$12"."id" = "entries$13"."id"
 INNER JOIN "product$10" ON "entries$13"."productid" = "product$10"."productid"