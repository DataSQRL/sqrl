Orders.entries.product := JOIN Product ON Product.productid = _.productid LIMIT 1;
Orders.entries.dProduct := SELECT DISTINCT category AS name FROM _.product LIMIT 1;


LogicalProject(id=[$0], _idx=[$1], name=[$2])
  LogicalSort(fetch=[1])
    LogicalProject(id=[$0], _idx=[$1], name=[$11])
      LogicalJoin(condition=[=($8, $2)], joinType=[default])
        LogicalTableScan(table=[[entries$13]])
        LogicalTableScan(table=[[product$10]])


SELECT *
FROM (SELECT "entries$13"."id", "entries$13"."_idx", "product$10"."category" AS "name"
  FROM entries$13
   DEFAULT JOIN product$10 ON "entries$13"."productid" = "product$10"."productid"
  FETCH NEXT 1 ROWS ONLY) AS "t0"