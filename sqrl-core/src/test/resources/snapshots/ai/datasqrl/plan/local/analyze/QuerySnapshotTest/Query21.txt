Customer := DISTINCT Customer ON customerid ORDER BY _ingest_time DESC;


LogicalProject(customerid=[$0], _uuid=[$1], _ingest_time=[$2], email=[$3], name=[$4], lastUpdated=[$5])
  LogicalSort(sort0=[$2], dir0=[DESC], fetch=[1])
    LogicalProject(customerid=[$2], _uuid=[$0], _ingest_time=[$1], email=[$3], name=[$4], lastUpdated=[$5])
      LogicalTableScan(table=[[customer$3]])


SELECT *
FROM (SELECT "customerid", "_uuid", "_ingest_time", "email", "name", "lastupdated" AS "lastUpdated"
  FROM "customer$3"
  ORDER BY "_ingest_time" DESC
  FETCH NEXT 1 ROWS ONLY) AS "t0"