/**
 * This package contains custom javax annotations used by DataSQRL configuration classes
 * for validations that aren't supported by existing javax annotations.
 *
 * Only add a new constraint if the validation logic cannot be accomplished with existing javax constraints.
 *
 */
package ai.datasqrl.config.constraints;

