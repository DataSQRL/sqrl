/**
 * This package contains classes for engine configuration options of DataSQRL server.
 *
 * Each engine (both database and stream engines) that is supported by DataSQRL server has a configuration class
 * through which users can customize the behavior of the engine.
 *
 * The {@link ai.datasqrl.physical} package contains the actual engine implementations, separated into database
 * and stream engine implementations. Consult the respective sub-packages for more information on a particular engine.
 *
 * @see ai.datasqrl.physical
 */
package ai.datasqrl.config.engines;