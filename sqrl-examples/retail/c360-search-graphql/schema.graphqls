type Product {
  productid: Int
  name: String
  description: String
  category: String
}

type Query {
  ProductSearch(query: String!): [Product]
  ProductSearchWithId(query: String!, id: Int!): [Product]
}

