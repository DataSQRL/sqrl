query UserFeed($user: Int!, $page: String) {
  users(filter: { id: {eq: $user}}, limit: 1)
  {
    data
    {
      stared_posts(order-by: [{recent_activity: DESC}], page: $page, page-size: 50)
      {
        id
        user
        {
          id
          name
          num_stars
        }
        num_stars
        num_connections
        title
        text
        timestamp
      }
    }
    page-state
    {
      has-next
      next-page
    }
  }
 }
