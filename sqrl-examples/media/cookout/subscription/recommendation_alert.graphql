subscription UserRecommendationAlert($userid: Int!) {
  UserRecommendationIsLive(filter: {userid: {eq: $userid}}) {
    stream
    {
      streamid
      user
      {
        id
        name
      }
      title
      tags
    }
  }
}
