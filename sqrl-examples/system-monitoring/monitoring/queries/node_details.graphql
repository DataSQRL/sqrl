query NodeDetails($cluster: Int!, $node: String!, $page: String) {
   clusters(filter: { clusterid: {eq: $cluster}}, limit: 1)
   {
     data
     {
       nodes (filter: { nodeid: {eq: $node}}, limit: 1)
       {
         last_config
         {
           cores
           memory_mb
           storage_mb
           db_version
         }
       }
     }
   }
 }
