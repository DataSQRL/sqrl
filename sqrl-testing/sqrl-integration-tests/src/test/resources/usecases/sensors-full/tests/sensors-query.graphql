query {
    Sensors(id: 9, limit: 10) {
        id
        machineId
        placed
        lastHour {
            maxTemp
            avgTemp
        }
        readings(limit: 5) {
            sensorid
            temp
        }
    }
}