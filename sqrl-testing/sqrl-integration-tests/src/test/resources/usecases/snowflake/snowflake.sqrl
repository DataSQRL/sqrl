IMPORT data.applications AS _applications;

-- Todo: snowflake stage gets elimiated if no queries ..?

MyIcebergTable := SELECT * FROM _applications;

SnowflakeQuery(@customerId: BIGINT) := SELECT * FROM MyIcebergTable WHERE customer_id = @customerId;