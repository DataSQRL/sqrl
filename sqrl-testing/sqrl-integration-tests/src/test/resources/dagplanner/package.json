{
  "version": "1",
  "connectors": {
    "kafka": {
      "topic" : "kafka-${sqrl:table-name}"
    },
    "kafka-mutation": {
      "topic" : "kafka-mutation-${sqrl:table-name}"
    },
    "postgres": {
      "table-name": "${sqrl:table-id}"
    }
  }
}