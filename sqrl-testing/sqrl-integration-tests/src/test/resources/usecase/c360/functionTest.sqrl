IMPORT time.*;
IMPORT ecommerce-data.Customer TIMESTAMP _ingest_time;
IMPORT ecommerce-data.Orders TIMESTAMP time;
IMPORT ecommerce-data.Product TIMESTAMP _ingest_time;
IMPORT text.*;
Product.badWords := bannedWordsFilter(name);
Product.searchResult := textsearch('garden gnome', description, name);
Product.format := format('Go buy: %s in %s with id=%s', name, category, CAST(productid AS STRING));
