>>>calcite
SELECT JSONEXTRACT(TOJSON('{"a": 0.2}'), '$.a', 0)
FROM "jsondata$2"
>>>postgres
SELECT COALESCE(CAST(JSONB_PATH_QUERY_FIRST('{"a": 0.2}', '$.a') AS INTEGER), 0)
FROM "jsondata$2"
>>>Postgres Result
0
>>>flink
SELECT JSONEXTRACT(TOJSON('{"a": 0.2}'), '$.a', 0)
FROM `jsondata$2`
>>>Flink Result
0
