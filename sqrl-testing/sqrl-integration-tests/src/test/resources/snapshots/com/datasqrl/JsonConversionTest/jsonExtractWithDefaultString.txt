>>>calcite
SELECT JSONEXTRACT(TOJSON('{"a": "hello"}'), '$.b', 'defaultString')
FROM "jsondata$2"
>>>postgres
SELECT COALESCE(JSONB_PATH_QUERY_FIRST('{"a": "hello"}', '$.b') #>> '{}', 'defaultString')
FROM "jsondata$2"
>>>Postgres Result
defaultString
>>>flink
SELECT JSONEXTRACT(TOJSON('{"a": "hello"}'), '$.b', 'defaultString')
FROM `jsondata$2`
>>>Flink Result
defaultString
