>>>calcite
SELECT JSONEXTRACT(CAST('{}' AS JSONB), '$.a', 'default')
FROM "jsondata$2"
>>>postgres
SELECT COALESCE(JSONB_PATH_QUERY_FIRST(CAST('{}' AS JSONB), '$.a') #>> '{}', 'default')
FROM "jsondata$2"
>>>Postgres Result
default
>>>flink
SELECT JSONEXTRACT(TOJSON('{}'), '$.a', 'default')
FROM `jsondata$2`
>>>Flink Result
default
