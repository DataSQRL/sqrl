>>>calcite
SELECT JSONEXTRACT(CAST('{"a": "hello"}' AS JSONB), '$.a', 'default')
FROM "jsondata$2"
>>>postgres
SELECT COALESCE(JSONB_PATH_QUERY_FIRST(CAST('{"a": "hello"}' AS JSONB), '$.a') #>> '{}', 'default')
FROM "jsondata$2"
>>>Postgres Result
hello
>>>flink
SELECT JSONEXTRACT(TOJSON('{"a": "hello"}'), '$.a', 'default')
FROM `jsondata$2`
>>>Flink Result
hello
