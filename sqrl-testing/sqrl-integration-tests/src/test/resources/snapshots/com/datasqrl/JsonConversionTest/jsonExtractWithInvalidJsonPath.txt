>>>calcite
SELECT JSONEXTRACT(CAST('{"a": "hello"}' AS JSONB), '$..', 'default')
FROM "jsondata$2"
>>>postgres
SELECT COALESCE(JSONB_PATH_QUERY_FIRST(CAST('{"a": "hello"}' AS JSONB), '$..') #>> '{}', 'default')
FROM "jsondata$2"
