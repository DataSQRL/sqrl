>>>calcite
SELECT JSONEXTRACT(TOJSON('{"a": "hello"}'), '$..', 'default')
FROM "jsondata$2"
>>>postgres
SELECT COALESCE(JSONB_PATH_QUERY_FIRST('{"a": "hello"}', '$..') #>> '{}', 'default')
FROM "jsondata$2"
