package com.datasqrl;

public class DataTypeIT {

  /**
   *
   *
   */

  /**
   * End to end with many data types
   * - For each connector, kafka + avro / kafka + json / file,
   * -
   * High level goal: we get a lot of testing out of one int test
   * - All data types, all parameters
   * Assume a remote cluster is running
   * 1. Create an avsc w/ all types
   * 2. Query those types in graphql
   *
   */

  /**
   * Testing 'framework'
   * 1. Package json construction via objects
   * 2.
   */

  public void test() {

  }
}
