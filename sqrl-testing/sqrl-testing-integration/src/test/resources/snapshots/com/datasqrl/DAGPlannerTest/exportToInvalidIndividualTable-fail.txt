[NOTICE] This table does not propagate the source row time columns: default_catalog.default_database.Orders[time], default_catalog.default_database.Customer[timestamp]
in script:exportToInvalidIndividualTable-fail.sqrl [3:1]:
IMPORT ecommerceTs.customer;
IMPORT ecommerceTs.orders;
JoinStream := SELECT o.id, c.name FROM Orders o JOIN Customer c on c.customerid = o.customerid;
^

[FATAL] Referenced table 'CustomerExportInternal' is not an external table
in script:sinks.sqrl [13:1]:

/*+ engine(kafka) */
CREATE TABLE CustomerExportInternal (
^
Using an individual table from another script in an `IMPORT` or `EXPORT` only supports external tables.

Learn more about external tables:
https://docs.datasqrl.com/docs/sqrl-language/#create-table-internal-vs-external

