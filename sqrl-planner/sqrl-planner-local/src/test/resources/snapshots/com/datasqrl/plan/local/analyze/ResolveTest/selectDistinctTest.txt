>>>customerid$q$18-lp-STREAM
LogicalProject(customerid=[$2], _ingest_time$0=[$1])
  LogicalTableScan(table=[[customer$q$2]])

>>>customerid$q$18-lp-DATABASE
LogicalProject(customerid=[$2], _ingest_time$0=[$1])
  LogicalFilter(condition=[=($6, 1)]) hints[DedupHint]
    LogicalProject(_uuid$0=[$0], _ingest_time$0=[$1], customerid$0=[$2], email$0=[$3], name$0=[$4], lastupdated$0=[$5], _rownum=[ROW_NUMBER() OVER (PARTITION BY $2 ORDER BY $1 DESC NULLS LAST)])
      LogicalTableScan(table=[[customer$3]])

>>>customerorders$q$21-lp-STREAM
LogicalProject(_uuid$0=[$0], id=[$2], customerid=[$6], time$0=[$4])
  LogicalJoin(condition=[=($3, $6)], joinType=[inner]) hints[TemporalJoinHint options:[4, 1, 0]]
    LogicalTableScan(table=[[orders$q$6]])
    LogicalFilter(condition=[=($2, 1)]) hints[DedupHint]
      LogicalProject(customerid=[$0], _ingest_time$0=[$1], _rownum=[ROW_NUMBER() OVER (PARTITION BY $0 ORDER BY $1 DESC NULLS LAST)])
        LogicalTableScan(table=[[customerid$q$18]])

>>>customerorders$q$21-lp-DATABASE
Stage [database] does not support capabilities: [TEMPORAL_JOIN]

