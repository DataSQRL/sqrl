Orders.products := SELECT p.* FROM @.entries e INNER JOIN Product AS p ON e.productid = p.productid;

LogicalProject(__pk_0=[$0], _uuid=[$11], _ingest_time=[$12], productid=[$13], name=[$14], description=[$15], category=[$16])
  LogicalJoin(condition=[=($7, $13)], joinType=[inner])
    LogicalJoin(condition=[=($0, $5)], joinType=[default])
      LogicalTableScan(table=[[orders$12]])
      LogicalTableScan(table=[[entries$13]])
    LogicalTableScan(table=[[product$10]])


SELECT "orders$12"."id" AS "__pk_0", "product$10"."_uuid", "product$10"."_ingest_time", "product$10"."productid", "product$10"."name", "product$10"."description", "product$10"."category"
FROM "orders$12"
 DEFAULT JOIN "entries$13" ON "orders$12"."id" = "entries$13"."id"
 INNER JOIN "product$10" ON "entries$13"."productid" = "product$10"."productid"