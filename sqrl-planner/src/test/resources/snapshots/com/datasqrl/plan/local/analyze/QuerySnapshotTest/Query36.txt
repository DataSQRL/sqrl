Orders.entries.product := JOIN Product ON Product.productid = @.productid LIMIT 1;
Orders.entries.dProduct := SELECT unit_price, product.category, product.name FROM @;


LogicalProject(__pk_0=[$0], __pk_1=[$1], unit_price=[$4], category=[$11], name=[$9])
  LogicalJoin(condition=[=($8, $2)], joinType=[left])
    LogicalTableScan(table=[[entries$13]])
    LogicalTableScan(table=[[product$10]])


SELECT "entries$13"."id" AS "__pk_0", "entries$13"."_idx" AS "__pk_1", "entries$13"."unit_price", "product$10"."category", "product$10"."name"
FROM "entries$13"
 LEFT JOIN "product$10" ON "entries$13"."productid" = "product$10"."productid"