An individual `EXPORT` target from another script must specify either a `connector` table option or an `engine` hint.

Learn more about external tables:
https://docs.datasqrl.com/docs/sqrl-language/#create-table-internal-vs-external
