query LocationDetails($location: Int!) {
   locations(filter: { id: {eq: $location}}, limit: 1)
   {
     data
     {
       name
       capacity
       occupancy
       occupancy_rate
     }
   }
 }
