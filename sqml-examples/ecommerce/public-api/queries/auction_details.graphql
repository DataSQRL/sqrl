query AuctionDetails($id: Int!, $page: String) {
   auctions(filter: { id: {eq: $id}}, limit: 1)
   {
     data
     {
       id
       item_name
       category
       {
         name
       }
       seller
       {
         id
         name
         num_auctions
       }
       start_time
       end_time
       start_price
       highest_bid_price
       highest_bidder
       {
         id
         name
       }
       bids (order-by: {timestamp: DESC}, page: $page, page-size: 20) {
         timestamp
         bid_price
         bidder
         {
           id
           name
         }
       }
     }
     page-state
     {
       has-next
       next-page
     }
   }
 }
