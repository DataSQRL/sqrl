query CategoryOpenAuctions($category: Int!, $time: Datetime!, $state: String!, $page: String) {
   categories(filter: { category_id: {eq: $category}}, limit: 1)
   {
     data
     {
       auctions(filter: { end_time: {gt: $time}}, order-by: {end_time: ASC}, page: $page, page-size: 25)
       {
         id
         item_name
         category
         {
           name
         }
         seller
         {
           id
           name
         }
         start_time
         end_time
         start_price
         highest_bid_price
         highest_bidder
         {
           id
           name
         }
       }
     }
     page-state
     {
       has-next
       next-page
     }
   }
 }
